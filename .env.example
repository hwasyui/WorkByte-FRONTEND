# Copy to .env and point at whichever backend this machine should talk to.
# No trailing slash - the services strip it, but keep it clean anyway.
#   local backend : http://10.0.2.2:8000   (Android emulator -> host)
#   local backend : http://localhost:8000  (web)
BACKEND=
